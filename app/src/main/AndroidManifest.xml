<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.studysmart">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreateDeckActivity" />
        <activity android:name=".ViewCollectionActivity" />
        <activity android:name=".DeckCollectionActivity" />
        <activity android:name=".DuelActivity" />
        <activity android:name=".StudyActivity" />
        <activity android:name=".AddFlashCardActivity" />
        <activity android:name=".DeckDetailsActivity" />
        <activity android:name=".RenameDeckActivity" />
        <activity android:name=".ViewDeckActivity" />
        <activity android:name=".AnswerCardActivity" />
        <activity android:name=".AddCardToCurrentDeckActivity"></activity>
        <activity android:name = ".ViewDetailsActivity" />
    </application>

</manifest>